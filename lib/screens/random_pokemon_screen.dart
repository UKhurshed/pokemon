import 'package:flutter/material.dart';

class RandomPokemonScreen extends StatelessWidget {
  const RandomPokemonScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
